# init

## Overview

Initialise the contents of `dim.json` and `dim-lock.json`.\
Files under `data_files` are not initialized.

```bash
dim init
```

## Examples

```bash
# Initialise the dim.
dim init
```
