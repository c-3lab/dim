# init

## Overview

Initialize the contents of `dim.json` and `dim-lock.json`.\
The `data_files` directory is not initialized.

```bash
dim init
```

## Examples

```bash
# Initialize the dim.
dim init
```
